const {resolve} = require('node:path');

//configurações do sequelize, como o caminho para o arquivo de configuração do banco de dados, o caminho para as migrations e o caminho para os modelos
module.exports = {
    config: resolve(__dirname, 'src', 'config', 'database.cjs'), //caminho para o arquivo de configuração do banco de dados
    'migrations-path': resolve(__dirname, 'src', 'database', 'migrations'), //caminho para as migrations
    'models-path': resolve(__dirname, 'src', 'app', 'models'), //caminho para os modelos
}